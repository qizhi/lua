<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.cubeia.firebase.test.systest</groupId>
	<artifactId>blackbox</artifactId>
	<packaging>jar</packaging>
	<name>Firebase Blackbox System Test</name>
	
	<parent>
		<groupId>com.cubeia.firebase.test</groupId>
		<artifactId>systest</artifactId>
		<version>1.9.14-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	
	<profiles>
		<!-- Warning, the "automated system test" config is used as soon a "testng-auto.xml"
		     file exists at all in the file system, remove this to run the tests manually.  -->
		<profile>
			<id>auto</id>
			<properties>
				<testng-file>testng-auto.xml</testng-file>
			</properties>
			<activation>
				<file>
					<exists>testng-auto.xml</exists>
				</file>
			</activation>
		</profile>
		<profile>
			<id>manual</id>
			<properties>
				<testng-file>testng.xml</testng-file>
			</properties>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
		</profile>
	</profiles>

	<dependencies>
		<dependency>
			<groupId>com.cubeia.firebase.test</groupId>
			<artifactId>common</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.cubeia.firebase.test.systest</groupId>
			<artifactId>game</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.cubeia.firebase.test.systest</groupId>
			<artifactId>service</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.cubeia.firebase.test.systest</groupId>
			<artifactId>tournament</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.cubeia.firebase</groupId>
			<artifactId>firebase-protocol</artifactId>
		</dependency>
		<dependency>
			<groupId>com.cubeia.firebase</groupId>
			<artifactId>firebase-api</artifactId>
		</dependency>
		<dependency>
			<groupId>com.cubeia.firebase</groupId>
			<artifactId>firebase-jmx</artifactId>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.4.2</version>
				<configuration>
					<suiteXmlFiles>
						<suiteXmlFile>${testng-file}</suiteXmlFile>
					</suiteXmlFiles>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
