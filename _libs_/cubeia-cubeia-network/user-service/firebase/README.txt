FIREBASE USER SERVICE INTEGRATION

= Configuration =

Specify the following properties in cluster.props:
com.cubeia.network.userservice.base-url=http://localhost:9090/user-service

You can always override this property by setting a Java system property with the same key.

